setup:
  addons:
    - plan: heroku-postgresql
build:
  docker:
    web: ch4-bookstore/Dockerfile
release:
  image: web
  command:
    - python manage.py collectstatic --noinput
run:
  web: gunicorn ch4-bookstore/django_project.wsgi:application
